---
phase: 02-domain-search-and-landing-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/hero-section.tsx
  - src/components/landing/tagline-strip.tsx
  - src/components/landing/services-strip.tsx
  - src/components/landing/how-it-works.tsx
  - src/components/landing/features-grid.tsx
  - src/components/landing/taco-specials.tsx
  - src/components/landing/pricing-section.tsx
  - src/components/landing/footer.tsx
  - src/app/globals.css
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "Landing page displays hero section with orb background, logo, headline, subtitle, and CTA buttons"
    - "Tagline strip scrolls horizontally with infinite CSS animation"
    - "Services strip shows 5 service cards in responsive grid"
    - "How-it-works shows 3 numbered steps with CSS counters"
    - "Features grid displays 10 feature cards with hover effects and tag badges (HOT, FREE, NEW, PRO)"
    - "Taco specials shows 4 bonus feature cards with gradient backgrounds"
    - "Pricing section shows 3 tiers (Mild/Medium/Extra Spicy) with Medium featured as most popular"
    - "Footer shows 4-column grid with brand, products, resources, company links"
  artifacts:
    - path: "src/components/landing/hero-section.tsx"
      provides: "Hero with orb background, chip badge, logo, h1, subtitle, CTA buttons, search bar slot"
      min_lines: 40
    - path: "src/components/landing/tagline-strip.tsx"
      provides: "Infinite scrolling tagline strip"
      min_lines: 15
    - path: "src/components/landing/services-strip.tsx"
      provides: "5 service cards grid"
      min_lines: 20
    - path: "src/components/landing/how-it-works.tsx"
      provides: "3-step process section"
      min_lines: 20
    - path: "src/components/landing/features-grid.tsx"
      provides: "10 feature cards with tags"
      min_lines: 40
    - path: "src/components/landing/taco-specials.tsx"
      provides: "4 bonus feature cards"
      min_lines: 20
    - path: "src/components/landing/pricing-section.tsx"
      provides: "3-tier pricing cards"
      min_lines: 40
    - path: "src/components/landing/footer.tsx"
      provides: "4-column footer with links"
      min_lines: 30
  key_links:
    - from: "src/components/landing/hero-section.tsx"
      to: "children prop"
      via: "React children slot for search bar injection"
      pattern: "children.*ReactNode"
    - from: "src/app/globals.css"
      to: "CSS animations"
      via: "@keyframes for orbFloat, fadeUp, scrollX, pulse"
      pattern: "@keyframes orbFloat"
---

<objective>
Port all 8 landing page sections from index.html to React server components, plus add CSS animations to globals.css. The hero section accepts a `children` slot where the search bar will be injected by Plan 03.

Purpose: Landing page sections are static content ideal for server components. Building them in parallel with Plan 01 (search logic) means the UI shell is ready before search functionality is wired in.

Output: 8 landing page component files + CSS animations in globals.css. The page.tsx composition will happen in Plan 03 after the search bar is ready.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-domain-search-and-landing-page/02-RESEARCH.md
@index.html
@src/app/globals.css
@src/app/(public)/layout.tsx
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/lib/utils.ts
</context>

<interfaces>
<!-- Existing UI components to use -->

From src/components/ui/button.tsx:
```typescript
export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: "primary" | "secondary" | "ghost" | "destructive";
  size?: "sm" | "md" | "lg";
  loading?: boolean;
}
```

From src/components/ui/card.tsx:
```typescript
export function Card({ className, ...props }: HTMLAttributes<HTMLDivElement>)
export function CardHeader({ className, ...props }: HTMLAttributes<HTMLDivElement>)
export function CardContent({ className, ...props }: HTMLAttributes<HTMLDivElement>)
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CSS animations + Hero + Tagline + Services + How-It-Works</name>
  <files>src/app/globals.css, src/components/landing/hero-section.tsx, src/components/landing/tagline-strip.tsx, src/components/landing/services-strip.tsx, src/components/landing/how-it-works.tsx</files>
  <action>
1. **Add CSS animations to `src/app/globals.css`** (append after existing content):
   ```
   @keyframes orbFloat {
     0%, 100% { transform: translate(0, 0) scale(1) }
     25% { transform: translate(30px, -40px) scale(1.05) }
     50% { transform: translate(-20px, 30px) scale(0.95) }
     75% { transform: translate(40px, 20px) scale(1.03) }
   }
   @keyframes fadeUp {
     from { opacity: 0; transform: translateY(28px) }
     to { opacity: 1; transform: translateY(0) }
   }
   @keyframes scrollX {
     from { transform: translateX(0) }
     to { transform: translateX(-50%) }
   }
   @keyframes pulse {
     0%, 100% { opacity: 1; transform: scale(1) }
     50% { opacity: 0.5; transform: scale(1.4) }
   }
   ```

2. **Create `src/components/landing/hero-section.tsx`** (server component, NO "use client"):
   - Accept `children?: React.ReactNode` prop for search bar injection
   - Orb background: 3 absolutely-positioned divs with blur(120px), opacity-[0.12], orbFloat animation (20s ease-in-out infinite with different animation-delays: 0s, -7s, -14s). Colors: fire (#ff5722), gold (#ffc107), cyan (#00e5ff). Sizes: 600px, 500px, 350px.
   - Chip badge: inline-flex pill with pulse dot + "Now serving .taco domains"
   - Logo: `<Image src="/taco_logo.png" width={140} height={140} />` with rounded-[28px] and drop-shadow
   - H1: "Get Your Domain. No Salsa Required." with fire gradient on "Domain." using `bg-gradient-to-r from-fire via-fire-light to-gold bg-clip-text text-transparent`
   - Subtitle paragraph
   - CTA buttons row: "Search Your Domain" (primary, links to #search or scrolls) and "How It Works" (outline)
   - `{children}` rendered below CTAs (this is where search bar goes)
   - Use fadeUp animation with staggered animation-delay on each element
   - Responsive: `text-[clamp(2.8rem,7.5vw,6rem)]` for h1, stack on mobile

3. **Create `src/components/landing/tagline-strip.tsx`** (server component):
   - Full-width strip with bg-surface, border-y border-border, overflow-hidden, py-5
   - Inner div with flex, scrollX animation 30s linear infinite, width: max-content
   - 6 taglines, DUPLICATED for seamless loop (12 spans total). Content from index.html:
     - "Don't be shellfish -- claim your domain today!"
     - "Hot domains. Fresh ideas. Zero crumbs."
     - "Your website deserves more than a boring burrito."
     - "Domain registration with extra guac (optional)"
     - "Even tacos need security. Get SSL today."
     - "Street tacos meets Silicon Valley."
   - Each span: font-anybody, text-text3, highlighted word in text-fire
   - Use `whitespace-nowrap px-10` for spacing

4. **Create `src/components/landing/services-strip.tsx`** (server component):
   - bg-bg2, border-y border-border, py-[60px] px-6
   - Inner container max-w-[1240px] mx-auto
   - Grid: `grid-cols-[repeat(auto-fit,minmax(220px,1fr))] gap-7`
   - 5 service cards from index.html, each with emoji icon (text-[40px]), h4 title, p description:
     1. Domain Registration (emoji: globe/world)
     2. Domain Brainstorming (emoji: lightbulb)
     3. Brandable Marketplace (emoji: store)
     4. Web Hosting (emoji: cloud)
     5. SSL Certificates (emoji: lock)
   - Cards: text-center py-8 px-5

5. **Create `src/components/landing/how-it-works.tsx`** (server component):
   - Section with section label ("// It's easy, promise"), title ("How It Works"), description
   - Use CSS class names for section label: font-mono text-[11px] uppercase tracking-[3px] text-fire
   - Grid: `grid-cols-[repeat(auto-fit,minmax(240px,1fr))] gap-6`, counter-reset via CSS
   - 3 step cards with: large faded counter number (::before pseudo via CSS class), emoji icon, h4, p
     1. Search - magnifying glass emoji
     2. Register - cart emoji
     3. Launch - rocket emoji
   - Cards: bg-bg2, border border-border, rounded-[--radius-default], p-8, text-center
   - Hover: border-fire, translateY(-3px)
   - Add counter CSS to globals.css if needed, or use explicit "01" / "02" / "03" text with large faded styling
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "TypeCheck passed"</automated>
  </verify>
  <done>Hero section renders with orb background, animated chip, logo, headline, subtitle, CTA buttons, and children slot. Tagline strip scrolls infinitely. Services strip shows 5 cards. How-it-works shows 3 steps. All server components, no "use client".</done>
</task>

<task type="auto">
  <name>Task 2: Features grid + Taco specials + Pricing + Footer</name>
  <files>src/components/landing/features-grid.tsx, src/components/landing/taco-specials.tsx, src/components/landing/pricing-section.tsx, src/components/landing/footer.tsx</files>
  <action>
1. **Create `src/components/landing/features-grid.tsx`** (server component):
   - Section label ("// All the toppings"), title ("Everything You Need"), description
   - Grid: `grid-cols-[repeat(auto-fill,minmax(350px,1fr))] gap-4`
   - 10 feature cards from index.html, each with:
     - Emoji icon (32px), h3 title, p description, optional tag badge
     - Tags: HOT (bg-red/10 text-red), FREE (bg-cyan/10 text-cyan), NEW (bg-fire/10 text-fire), PRO (bg-gold/10 text-gold)
     - Badge: inline-block text-[10px] font-bold uppercase tracking-[1px] px-2.5 py-0.5 rounded-full mt-3.5
   - Cards: bg-bg2 border border-border rounded-[--radius-default] p-[34px_30px] relative overflow-hidden
   - Hover effect: use group class, `before:content-[''] before:absolute before:top-0 before:left-0 before:right-0 before:h-[3px] before:bg-gradient-to-r before:from-fire before:via-[#ff9800] before:to-[#ffc107] before:scale-x-0 before:origin-left before:transition-transform before:duration-400 group-hover:before:scale-x-100`
   - Hover: bg-bg3, border-fire/20, -translate-y-[3px], shadow
   - Features list (exact from index.html):
     1. Domain Search & Registration (HOT)
     2. Management Dashboard
     3. DNS Hosting & Management (FREE)
     4. Email Forwarding & MX
     5. SSL/TLS Certificates (FREE)
     6. Domain Marketplace (NEW)
     7. Shortlinks & Subdomains
     8. Developer API (PRO)
     9. Support & Education
     10. Billing & Subscriptions

2. **Create `src/components/landing/taco-specials.tsx`** (server component):
   - Section label ("// Secret menu"), title ("Taco Specials"), description
   - Grid: `grid-cols-[repeat(auto-fit,minmax(250px,1fr))] gap-4`
   - 4 cards from index.html:
     1. Spicy Mode (chili emoji) -- Advanced DNS settings for power users
     2. Taco Bell (bell emoji) -- Notification center
     3. Salsa Sync (dancer emoji) -- One-click DNS sync
     4. Guac Guard (avocado emoji) -- Premium security
   - Cards: bg-gradient-to-br from-bg2 to-surface, border border-border, rounded-[--radius-default], p-[30px_24px], text-center
   - Radial gradient overlay on hover (::after pseudo) -- use Tailwind `after:` classes or a separate div
   - Hover: -translate-y-[3px], border-fire

3. **Create `src/components/landing/pricing-section.tsx`** (server component):
   - Section label ("// Transparent pricing"), title ("Simple, Honest Pricing"), description
   - Grid: `grid-cols-[repeat(auto-fit,minmax(280px,1fr))] gap-5 items-start`
   - 3 pricing tiers from index.html:
     **Mild** ($0/mo, Free forever): 1 free domain (.xyz), Free DNS hosting, Free SSL certificate, Email forwarding (1 alias), Basic support. Button: "Start Free" (outline style)
     **Medium** ($9/mo, Billed annually, FEATURED): Up to 25 domains, Advanced DNS + DNSSEC, Wildcard SSL, Unlimited email forwards, Branded shortlinks, API access, Priority support. Button: "Get Medium" (primary/fire gradient). Has "MOST POPULAR" ribbon above card.
     **Extra Spicy** ($29/mo, Billed annually): Unlimited domains, All DNS + zone export, EV SSL certificates, Team billing & permissions, Marketplace seller tools, Full API + webhooks + SDKs, Dedicated support + SLA, All Taco Specials. Button: "Go Extra Spicy" (outline style)
   - Featured card: border-fire, bg gradient from fire/5 to bg2, ::before "MOST POPULAR" pill positioned absolute top-[-13px]
   - Feature list items: each with cyan checkmark prefix
   - Hover: -translate-y-[5px], shadow

4. **Create `src/components/landing/footer.tsx`** (server component):
   - border-t border-border, py-[72px] px-6, max-w-[1240px] mx-auto
   - 4-column grid (`grid-cols-[2fr_1fr_1fr_1fr] gap-10`), responsive to 2-col then 1-col
   - Column 1 (Brand): Logo image (32px height) + "Taco Domains" text, description paragraph
   - Column 2 (Products): Domain Search, DNS Hosting, Marketplace, Pricing, API -- use `<Link>` to appropriate routes or `#` placeholders
   - Column 3 (Resources): Taco Tips Blog, Documentation, Status Page, Discord Community, Changelog -- all `#` links for now
   - Column 4 (Company): About Us, Careers, Privacy Policy, Terms of Service, Contact -- all `#` links
   - Bottom bar: flex justify-between, border-t border-border, pt-7, text-[12px] text-text3
   - Left: copyright text, Right: "Made with salsa & love" in mono font
   - Responsive: `md:grid-cols-[2fr_1fr_1fr_1fr]`, below md: `grid-cols-2`, below sm: `grid-cols-1`
  </action>
  <verify>
    <automated>npx tsc --noEmit && echo "TypeCheck passed"</automated>
  </verify>
  <done>Features grid shows 10 cards with hover effects and tag badges. Taco specials shows 4 gradient cards. Pricing shows 3 tiers with Medium featured. Footer shows 4-column layout with links. All server components, no "use client".</done>
</task>

</tasks>

<verification>
Type check all components:
```bash
npx tsc --noEmit
```

Build check:
```bash
npm run build
```
Note: Components are not yet imported into page.tsx (that happens in Plan 03), so build verifies they compile without import errors.
</verification>

<success_criteria>
- All 8 landing page components created as server components (no "use client")
- CSS animations (orbFloat, fadeUp, scrollX, pulse) added to globals.css
- Hero section accepts children prop for search bar injection
- All content matches index.html (text, structure, layout)
- Components use existing Tailwind theme colors (fire, lime, cyan, gold, bg, surface, etc.)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-search-and-landing-page/02-02-SUMMARY.md`
</output>
