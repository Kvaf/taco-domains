---
phase: 01-foundation-and-authentication
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/card.tsx
  - src/components/ui/label.tsx
  - src/components/ui/avatar.tsx
  - src/components/layout/public-navbar.tsx
  - src/components/layout/dashboard-sidebar.tsx
  - src/components/layout/dashboard-header.tsx
  - src/app/(public)/layout.tsx
  - src/app/(public)/page.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Public pages show top navbar with logo and navigation links"
    - "Dashboard pages show collapsible sidebar with navigation items"
    - "Application renders correctly at 375px, 768px, and 1440px widths"
    - "Reusable UI components (Button, Input, Card) use taco theme colors"
    - "Dashboard shows sidebar items: Overview, My Domains, Settings; others marked Coming Soon"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Reusable Button with variants (primary/secondary/ghost/destructive) and sizes"
      exports: ["Button"]
    - path: "src/components/ui/input.tsx"
      provides: "Reusable Input with label, error state, and dark theme"
      exports: ["Input"]
    - path: "src/components/ui/card.tsx"
      provides: "Reusable Card container with header/content/footer sections"
      exports: ["Card", "CardHeader", "CardContent", "CardFooter"]
    - path: "src/components/layout/public-navbar.tsx"
      provides: "Top navigation bar for public pages with logo and CTA"
      exports: ["PublicNavbar"]
    - path: "src/components/layout/dashboard-sidebar.tsx"
      provides: "Dashboard sidebar with nav items, collapsible on mobile"
      exports: ["DashboardSidebar"]
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout with sidebar + main content area"
      min_lines: 20
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/layout/public-navbar.tsx"
      via: "imports and renders PublicNavbar"
      pattern: "PublicNavbar"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/dashboard-sidebar.tsx"
      via: "imports and renders DashboardSidebar"
      pattern: "DashboardSidebar"
---

<objective>
Build the reusable UI component library and application shell layouts (public navbar, dashboard sidebar) with responsive design.

Purpose: All pages in the application need consistent UI components and layout structure. Public pages use a top navbar; dashboard pages use a collapsible sidebar. Per locked decision from CONTEXT.md.
Output: Reusable Button/Input/Card/Label/Avatar components, public layout with top navbar, dashboard layout with sidebar.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-and-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-and-authentication/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

Tailwind theme colors available (from globals.css @theme):
- bg, bg2, bg3, surface, surface2 (backgrounds)
- border, border2 (borders)
- fire, fire-light, fire-dark (primary accent)
- lime, lime-dark, cyan, cyan-dark, gold, gold-dark (secondary accents)
- text, text2, text3, text4 (text hierarchy)

Fonts configured in layout.tsx:
- Anybody (headings via CSS variable)
- Outfit (body, default)
- JetBrains Mono (code/mono)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable UI components (Button, Input, Card, Label, Avatar)</name>
  <files>
    src/components/ui/button.tsx, src/components/ui/input.tsx,
    src/components/ui/card.tsx, src/components/ui/label.tsx,
    src/components/ui/avatar.tsx
  </files>
  <action>
    1. Create `src/components/ui/button.tsx`:
       - Props: variant ("primary" | "secondary" | "ghost" | "destructive"), size ("sm" | "md" | "lg"), disabled, loading, className, children, plus native button props
       - Primary variant: bg-fire text-black hover:bg-fire-light font-semibold
       - Secondary variant: bg-surface border border-border text-text hover:bg-surface2
       - Ghost variant: bg-transparent text-text2 hover:bg-surface hover:text-text
       - Destructive variant: bg-red/10 text-red border border-red/20 hover:bg-red/20
       - Sizes: sm (px-3 py-1.5 text-sm), md (px-4 py-2 text-base), lg (px-6 py-3 text-lg)
       - Loading state: show spinner icon, disable button
       - Rounded with radius-sm, transition-colors, focus-visible ring with fire color
       - Use cn() for class merging, forwardRef for ref forwarding

    2. Create `src/components/ui/input.tsx`:
       - Props: label (optional), error (optional string), className, plus native input props
       - Dark themed: bg-bg2 border-border text-text placeholder:text-text3
       - Focus: border-fire ring-1 ring-fire/30
       - Error state: border-red text-red, error message below in text-sm text-red
       - Label rendered above input in text-sm text-text2 font-medium
       - Rounded with radius-sm, px-3 py-2
       - Use forwardRef for form library compatibility

    3. Create `src/components/ui/card.tsx`:
       - Export Card, CardHeader, CardContent, CardFooter as composable components
       - Card: bg-surface border border-border rounded-[var(--radius-default)] overflow-hidden
       - CardHeader: px-6 py-4 border-b border-border
       - CardContent: px-6 py-4
       - CardFooter: px-6 py-4 border-t border-border bg-surface2/30
       - All accept className prop for customization

    4. Create `src/components/ui/label.tsx`:
       - Simple label component: text-sm font-medium text-text2
       - Accepts htmlFor and className props

    5. Create `src/components/ui/avatar.tsx`:
       - Props: name (string), image (optional string URL), size ("sm" | "md" | "lg")
       - If image provided: render img with rounded-full
       - If no image: render initials from name in a colored circle
       - Initials: first letter of first and last name (or first two letters if one word)
       - Background: use fire gradient for initials fallback
       - Sizes: sm (w-8 h-8 text-xs), md (w-10 h-10 text-sm), lg (w-12 h-12 text-base)
       - Per research: use initials/placeholder in Phase 1 (no file upload)
  </action>
  <verify>
    <automated>cd "C:/Users/kvaf8/Downloads/taco-domains/.claude/worktrees/confident-hawking" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>All 5 UI components export correctly, accept className for customization, use taco theme colors, TypeScript compiles with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Build public and dashboard layouts with responsive navigation</name>
  <files>
    src/components/layout/public-navbar.tsx, src/components/layout/dashboard-sidebar.tsx,
    src/components/layout/dashboard-header.tsx,
    src/app/(public)/layout.tsx, src/app/(public)/page.tsx,
    src/app/(dashboard)/layout.tsx, src/app/(dashboard)/dashboard/page.tsx
  </files>
  <action>
    1. Create `src/components/layout/public-navbar.tsx`:
       - Top navigation bar for public pages
       - Left: taco_logo.png + "Taco Domains" text (link to /)
       - Right: nav links (hidden on mobile, visible on md+): "Features", "Pricing" (placeholder hrefs)
       - Right: "Log In" link to /login, "Get Started" Button (primary) link to /signup
       - Mobile (below md): hamburger menu icon that toggles a dropdown/slide-down menu
       - Use "use client" for mobile menu toggle state
       - Sticky top, bg-bg/80 backdrop-blur-md border-b border-border z-50
       - Max-width container (max-w-7xl mx-auto px-4)
       - Use Lucide icons for hamburger (Menu) and close (X)

    2. Create `src/components/layout/dashboard-sidebar.tsx`:
       - Sidebar navigation for dashboard (per locked decision: sidebar layout like existing mockup)
       - "use client" for collapse/expand state
       - Logo at top linking to /dashboard
       - Nav items with icons (Lucide): Overview (LayoutDashboard), My Domains (Globe), DNS Records (Server), Marketplace (ShoppingBag), Settings (Settings)
       - Phase 1 active items: Overview, My Domains (empty state), Settings
       - Other items: visible but with "Coming Soon" badge (muted text, not clickable)
       - Active item: bg-surface2 text-fire border-l-2 border-fire
       - Inactive item: text-text2 hover:text-text hover:bg-surface
       - At bottom: user info (avatar + name) and logout button
       - Desktop (lg+): always visible, w-64
       - Tablet (md-lg): collapsible, icons only when collapsed (w-16)
       - Mobile (below md): hidden by default, slides in as overlay when hamburger clicked
       - Use a sidebar toggle button (visible on md, hidden on lg+)

    3. Create `src/components/layout/dashboard-header.tsx`:
       - Top bar for dashboard pages (visible on mobile and tablet when sidebar is hidden/collapsed)
       - Left: hamburger button to toggle sidebar (visible below lg)
       - Center/Left: page title (passed as prop or from route)
       - Right: user avatar + dropdown with "Settings" and "Log Out" links
       - Use Radix UI DropdownMenu for user menu
       - Sticky top within the main content area

    4. Create `src/app/(public)/layout.tsx`:
       - Renders PublicNavbar at top
       - Children below in main content area
       - Full dark background (bg-bg min-h-screen)

    5. Create `src/app/(public)/page.tsx`:
       - Placeholder landing page (Phase 2 will build the real one)
       - Hero text: "Get Your Domain. No Salsa Required." in Anybody font
       - Subtitle: "The spiciest domain registrar on the internet."
       - CTA buttons: "Search Domains" (primary, links to /signup for now) and "Log In" (secondary)
       - Centered content, fire gradient text on heading
       - This is a placeholder -- Phase 2 replaces it with the full landing page

    6. Create `src/app/(dashboard)/layout.tsx`:
       - Server Component that checks auth via getCurrentUser()
       - If not authenticated: redirect to /login (backup to middleware)
       - Renders DashboardSidebar + DashboardHeader + main content area
       - Layout: sidebar fixed left, main content fills remaining width
       - Desktop: sidebar always visible (w-64), main content ml-64
       - Mobile: sidebar hidden, full-width main content
       - Pass user data to sidebar and header components

    7. Create `src/app/(dashboard)/dashboard/page.tsx`:
       - Dashboard overview / empty state welcome page
       - Show welcome message: "Welcome to Taco Domains!" with taco emoji
       - Empty state: "Start by searching for your first domain" with CTA button
       - Display user's name from session
       - Card-based layout using the Card component
       - Stats cards (placeholder): "0 Domains", "0 Active", "0 Expiring Soon"
       - Responsive grid: 1 col on mobile, 2 on tablet, 3 on desktop
  </action>
  <verify>
    <automated>cd "C:/Users/kvaf8/Downloads/taco-domains/.claude/worktrees/confident-hawking" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Public pages render with top navbar (responsive hamburger on mobile). Dashboard renders with sidebar (collapsible). Landing placeholder and dashboard overview page visible. All layouts responsive at 375px/768px/1440px.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Visit / -- public navbar with logo, nav links, login/signup buttons visible
3. Visit /dashboard (logged in) -- sidebar with nav items, main content area with welcome message
4. Resize to 375px -- navbar shows hamburger, sidebar hidden, content full-width
5. Resize to 768px -- sidebar collapsible, content adapts
6. Resize to 1440px -- sidebar always visible, three-column stats grid
7. Button variants render correctly (primary=fire, secondary=surface, ghost=transparent)
</verification>

<success_criteria>
- Public pages: top navbar with logo, links, CTA buttons; responsive hamburger on mobile
- Dashboard: sidebar with Overview/My Domains/Settings active, DNS/Marketplace as "Coming Soon"
- Sidebar: collapsible on tablet, hidden on mobile with hamburger toggle
- UI components: Button (4 variants), Input (with error/label), Card (composable), Label, Avatar (initials fallback)
- All layouts render correctly at 375px, 768px, 1440px
- Taco theme colors used consistently (fire accent, dark backgrounds, text hierarchy)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-authentication/01-03-SUMMARY.md`
</output>
